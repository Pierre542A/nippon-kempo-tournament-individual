{
  "buildCommand": "ls -la && cd front-quasar && ls -la && npm install && npm run build",
  "outputDirectory": "front-quasar/dist/spa",
  "functions": {
    "api-fastify/index.js": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api-fastify/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/front-quasar/dist/spa/index.html"
    }
  ]
}